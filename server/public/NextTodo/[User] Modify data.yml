info:
  name: "[User] Modify data"
  type: http
  seq: 7

http:
  method: PATCH
  url: http://localhost:3000/api/user/me
  body:
    type: json
    data: '{ "isActive": "false" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
