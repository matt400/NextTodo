info:
  name: "[Tag] Get one"
  type: http
  seq: 28

http:
  method: GET
  url: http://localhost:3000/api/tags?single=true&tag_id=1
  params:
    - name: single
      value: 'true'
      type: query
    - name: tag_id
      value: '1'
      type: query
  body:
    type: json
    data: ''
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
