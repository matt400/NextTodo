info:
  name: "[Tag] Add to task"
  type: http
  seq: 32

http:
  method: POST
  url: http://localhost:3000/api/tags/tasks
  body:
    type: json
    data: '{ "task_id": 1, "tag_id": 1 }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
