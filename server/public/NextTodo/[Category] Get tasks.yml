info:
  name: "[Category] Get tasks"
  type: http
  seq: 26

http:
  method: GET
  url: http://localhost:3000/api/category/tasks?category_id=1&tag_id=5
  params:
    - name: category_id
      value: "1"
      type: query
    - name: tag_id
      value: "5"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
