info:
  name: "[Pomo] Resume event"
  type: http
  seq: 17

http:
  method: POST
  url: http://localhost:3000/api/task/resumePomo
  body:
    type: json
    data: '{ "task_id": 1 }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
