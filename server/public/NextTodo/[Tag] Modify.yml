info:
  name: "[Tag] Modify"
  type: http
  seq: 30

http:
  method: PATCH
  url: http://localhost:3000/api/tags
  body:
    type: json
    data: '{ "tag_id": 1, "data": { "name": "Personal", "color": "#22c55e" } }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
