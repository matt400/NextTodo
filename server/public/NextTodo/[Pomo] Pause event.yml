info:
  name: "[Pomo] Pause event"
  type: http
  seq: 16

http:
  method: POST
  url: http://localhost:3000/api/task/pausePomo
  body:
    type: json
    data: '{ "task_id": 1 }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
