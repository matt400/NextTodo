info:
  name: "[Tag] Remove"
  type: http
  seq: 31

http:
  method: DELETE
  url: http://localhost:3000/api/tags
  body:
    type: json
    data: '{ "tag_id": 1 }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
