info:
  name: "[Tag] Remove from task"
  type: http
  seq: 33

http:
  method: DELETE
  url: http://localhost:3000/api/tags/tasks
  body:
    type: json
    data: '{ "task_id": 1, "tag_id": 1 }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
