info:
  name: "[Category] Get all"
  type: http
  seq: 21

http:
  method: GET
  url: http://localhost:3000/api/category
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
