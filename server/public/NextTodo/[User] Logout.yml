info:
  name: "[User] Logout"
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/api/user/logout
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
